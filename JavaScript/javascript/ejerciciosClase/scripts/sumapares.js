var numeros = ["1","8","16","5"]
contador = 0;

function guardar(){

    newElem = document.getElementById("introducir").value ;
    parseFloat(numeros.push(newElem));
    document.getElementById("comunicar").innerHTML= "Se ha introducido correctamente el número "+ newElem + " en el Array";
}


function sumar(){

    for(i=0; i < numeros.length; i++){

        while(numeros[i] % 2 == 0){

            var Pares =[];
            Pares.push(numeros [i]);
        }
        
    } 

    while(i < Pares.length){

            acumulador = Pares[j];
            
            
        }
         

}

