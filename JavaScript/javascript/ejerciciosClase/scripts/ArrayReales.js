var Numeros =[];

function anadir(){

    var NewNumb = document.getElementById("new").value;
    Numeros.push(NewNumb);
    
}

function operar(){

    for(i=0; i<=Numeros.length; i++){
        
        sumador=Numeros[i];
        
        }

        acumulador = sumador + 
}
